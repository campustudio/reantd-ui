import React, { Component } from 'react';
import { AsyncDCInput } from '@lib';

export default class PartTypesConfirmSync extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }
  
  render() {
    const { partTypes = [] } = this.props;

    return (
      <div>
      </div>
    );
  }
}
